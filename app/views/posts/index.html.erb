<h1>BLAHG</h1>
<ul>
  <% @posts.each do |post|%>
  <li>
    <%= post.title %>
    <% if current_user.admin == true %>
      <%= link_to "Edit", edit_post_path(post) %>
    <% end %>
  </li>
  <% end %>
</ul>

<%= link_to "Add post", new_post_path %>

